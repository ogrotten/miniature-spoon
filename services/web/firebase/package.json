{
  "name": "web-firebase",
  "version": "1.0.0",
  "description": "CNDW firebase",
  "main": "index.js",
  "scripts": {
    "dev": "firebase serve",
    "deploy": "firebase deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "firebase:login": "firebase login --no-localhost"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "firebase-tools": "^9.16.0"
  }
}
